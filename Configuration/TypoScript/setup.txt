# Module configuration
module.tx_freemind2 {
	persistence {
		storagePid = {$module.tx_freemind2.persistence.storagePid}
	}
	view {
		templateRootPath = {$module.tx_freemind2.view.templateRootPath}
		partialRootPath = {$module.tx_freemind2.view.partialRootPath}
		layoutRootPath = {$module.tx_freemind2.view.layoutRootPath}
	}
	
	settings {
	
		# this values comes out of the freemind.properties file, unpack the lib/freemind.jar file in our FreeMind installation
		# official buildin icons change only the filename if you know what to do
		# path below Resources/Public/ Supports only png files
		iconsPath = freemind_icons/

		userIconsPath = fileadmin/nuvola/16x16/filesystems/
		
		# first option is always default
		edgeStyles = bezier,linear,sharp_bezier,sharp_linear
		edgeWidths = parent,thin,1,2,4,8
		nodePositions = right,left
		nodeStyles = bubble,fork
	}
}